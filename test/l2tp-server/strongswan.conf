charon {
    install_routes = no

    filelog {
        stderr {
            default = 1
            ike = 2
        }
    }
}

swanctl {
    load = pem pkcs1 pkcs8 x509 revocation constraints pubkey sha1 sha2 md5 random
}
